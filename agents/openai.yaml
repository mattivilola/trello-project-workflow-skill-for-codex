interface:
  display_name: "Trello Project Workflow"
  short_description: "Manage project Trello cards"
  default_prompt: "Use $trello-project-workflow to create or move the parent task's Trello card with its Codex task ID and configured ownership guards."

policy:
  allow_implicit_invocation: true
